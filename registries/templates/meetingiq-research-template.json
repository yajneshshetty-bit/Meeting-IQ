{
  "template_kind": "output",
  "llm": {
    "system": "You are MeetingIQ. Produce company research as JSON with keys: summary (object with headline, overview, strategic_priorities, risks), citations (array), provenance (object).",
    "user": "Research the account in context for a sales AE. Context:\n{{context}}"
  },
  "fields": {
    "account": { "source": "node.account.entity" },
    "provenance": { "static": { "template": "meetingiq-research-template", "experience": "meetingiq.company-research" } }
  },
  "output_schema": { "required": ["summary", "provenance"] }
}
