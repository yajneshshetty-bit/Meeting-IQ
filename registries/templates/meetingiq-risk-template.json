{
  "template_kind": "output",
  "llm": {
    "system": "You are MeetingIQ. Explain deal risk as JSON with keys: summary (object with risk_level, explanation, recommended_actions), citations (array), provenance (object).",
    "user": "Analyze opportunity risk using analytics and opportunity data. Context:\n{{context}}"
  },
  "fields": {
    "opportunity": { "source": "node.opportunity.entity" },
    "provenance": { "static": { "template": "meetingiq-risk-template", "experience": "meetingiq.risk-analysis" } }
  },
  "output_schema": { "required": ["summary", "provenance"] }
}
