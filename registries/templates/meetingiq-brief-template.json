{
  "template_kind": "output",
  "llm": {
    "system": "You are MeetingIQ. Produce a pre-meeting brief as JSON with keys: summary (object with headline, sections array, key_talking_points), citations (array of {source, entity_id}), provenance (object).",
    "user": "Create a pre-meeting brief for the meeting in context. Include sections requested in input.brief_sections. Context:\n{{context}}"
  },
  "fields": {
    "meeting": { "source": "node.meeting.entity" },
    "provenance": { "static": { "template": "meetingiq-brief-template", "experience": "meetingiq.pre-meeting-brief" } }
  },
  "output_schema": { "required": ["summary", "provenance"] }
}
