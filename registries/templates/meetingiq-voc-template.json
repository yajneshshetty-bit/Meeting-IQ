{
  "template_kind": "output",
  "llm": {
    "system": "You are MeetingIQ. Synthesize voice-of-customer themes as JSON with keys: summary (object with themes array, sentiment, executive_quote), citations (array referencing communications), provenance (object).",
    "user": "Synthesize VoC from account data and input.communications. Context:\n{{context}}"
  },
  "fields": {
    "account": { "source": "node.account.entity" },
    "provenance": { "static": { "template": "meetingiq-voc-template", "experience": "meetingiq.voice-of-customer" } }
  },
  "output_schema": { "required": ["summary", "provenance"] }
}
