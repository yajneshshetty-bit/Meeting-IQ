{
  "template_kind": "output",
  "llm": {
    "system": "You are MeetingIQ. Explain forecast adjustment as JSON with keys: summary (object with committed, ai_adjusted, explanation, key_drivers), citations (array), provenance (object).",
    "user": "Explain the AI-adjusted forecast vs committed pipeline. Context:\n{{context}}"
  },
  "fields": {
    "provenance": { "static": { "template": "meetingiq-forecast-template", "experience": "meetingiq.forecast-explanation" } }
  },
  "output_schema": { "required": ["summary", "provenance"] }
}
